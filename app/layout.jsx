export default function Layout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>learn next.js fundamental</title>
      </head>
      <body>{children}</body>
    </html>
  );
}
